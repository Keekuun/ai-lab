# 本实验不调用模型，只演示能力发现、Schema 和权限。
OPENAI_API_KEY=

# HTTP 入口可选：成对设置后，请求必须带 Authorization: Bearer <token>
# 角色由 token 决定，不再用 MCP_ACTOR_ROLE。
MCP_HTTP_READER_TOKEN=
MCP_HTTP_WRITER_TOKEN=

# 可选：发布的帖子落盘路径，进程重启后仍能检索。
MCP_STORE_PATH=
